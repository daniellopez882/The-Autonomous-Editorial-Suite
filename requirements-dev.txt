# Development and CI dependencies. Installing this also installs the runtime set.
-r requirements.txt

pytest>=9.0.3,<10.0
pytest-cov>=5.0,<8.0
ruff>=0.6,<1.0
# black removed: ruff format is the single formatter. Running both meant
# local and CI could disagree depending on which black version resolved.
bandit>=1.7.9,<2.0
